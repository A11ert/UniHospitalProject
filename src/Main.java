import menu.TerminalInfo;

public class Main {
    public static void main(String[] args) {
        boolean on=true;
        TerminalInfo terminal = new TerminalInfo();
        terminal.run();
    }
}